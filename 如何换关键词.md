# 如何换关键词（How to change keywords）

> 一句话结论：**换关键词 = 改 `config/topics/` 下的 YAML，完全不用动 `src/` 代码。**
> 项目铁律：行业内容（公司名 / 关键词 / 地区 / 官网域名 / 指标）一律来自 config，核心代码零行业硬编码。

---

## 1. 关键词存在哪

`config/topics/<topic_id>.yaml` 的 `keywords:` 段，分 6 组：

| 组 | 作用 | charging_pile 当前值举例 |
|---|---|---|
| `core` | **大方向锚点**（必填，最关键） | 充电桩、充电基础设施 |
| `products` | 产品 / 服务词 | 液冷超充、光储充一体化 |
| `market` | 市场 / 经营词 | 保有量、运营商 |
| `technology` | 技术词 | 大功率快充、V2G |
| `events` | 事件词 | 政策、招标、补贴 |
| `exclude` | 排除词 | 招聘、二手车 |

配套还有（换关键词时常常要一起改）：

- `entities.companies` — 关注企业（`canonical_name` + `aliases`，别名用于归一化去重）
- `official_domains` — 官网 `site:` 检索域名
- `metrics` — 关注的指标（id 见 `config/taxonomies/metrics.yaml`）
- `scope.regions` — 地区

---

## 2. 三种换法，按需求选

### ① 临时试跑（不改文件）—— 用 `--focus` 覆盖核心词

```bash
.venv/Scripts/python.exe main.py --topic charging_pile --task charging_cn_weekly \
  --focus 换电站,无线充电 --notify true
```

- `--focus` 只覆盖核心词；`--regions` / `--companies` 同理覆盖地区 / 企业。
- 优先级：`--focus` 命令行 > `task.overrides.focus` > `topic.keywords.core`。
- 一次性，配置文件不动，下次运行恢复默认。

### ② 长期换方向（改现有 topic）—— 编辑 `charging_pile.yaml`

```yaml
keywords:
  core:            # ← 最主要改这里，它决定 LLM 生成什么热点
    - 换电站
    - 无线充电
  products:
    - ...
```

通常配套改 `entities.companies`、`official_domains`、`metrics`。

### ③ 换一个全新行业 —— 复制模板建新 topic + task

```bash
cp config/topics/_template.yaml config/topics/储能.yaml          # 填 id/name/regions/keywords/企业/官网域名/指标
cp config/tasks/_template.yaml   config/tasks/储能周报.yaml       # task.topic_id 指向新 topic
```

---

## 3. 关键认知（v0.7.0a5 之后）

**关键词只是"大方向锚点"，不是写死的搜索词。**

- 检索先由 LLM 基于 `core` 动态生成当前行业热点，优先搜热点；
- `products` / `technology` / `market` / `events` 降级为兜底固定查询 + 相关性门控信号词。

所以：

| 目的 | 改哪 |
|---|---|
| 换**方向** | `core`（影响最大） |
| 锁定某类内容不被门控误杀 | 那几组信号词跟 `core` 配套 |
| 排除噪音 | `exclude` |

---

## 4. 改完必须校验

```bash
.venv/Scripts/python.exe main.py --validate
```

会校验所有 topic/task 配置是否合法（`topic.id` 必须等于文件名、`scope.regions` 和 `keywords.core` 必填等）。

---

## 5. 数据提醒

- 换**新 topic_id**：干净起点，全新采集。
- 同一 topic_id 内改关键词：复用旧采集数据（`data/collection.jsonl` + SQLite），想清空重来加 `--rebuild-db`。
