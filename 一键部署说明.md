# 一键自动化部署

本项目的生产运行环境是 **GitHub Actions 云端**。双击根目录的 `一键部署.cmd`，脚本会依次完成：

1. 创建项目独立 `.venv` 并安装依赖；
2. 执行配置校验、416 项测试、Ruff 和 mypy；
3. 下载项目内便携版 GitHub CLI（不做全局安装）；
4. 发起一次 GitHub 浏览器授权；
5. 创建或复用私有仓库、提交并推送代码；
6. 启用工作流，同时保留仓库现有的 Actions 白名单与默认令牌权限；
7. 隐藏输入并加密上传 `DEEPSEEK_API_KEY`、`SERVERCHAN_KEY`；
8. 触发首次 `Manual Run` 验收。

密钥仅在内存中短暂存在，经标准输入交给 GitHub CLI，不写入 `.env`、日志或 Git 历史。默认仓库名为 `你的账号/industry-intelligence-agent`，默认可见性为 Private。

## 前置条件

- Windows 10/11；
- Git for Windows；
- Python 3.11+（推荐 3.12）。

如果缺少 Git 或 Python，交互模式会询问是否用 `winget` 安装；不会静默修改系统。GitHub CLI 会下载到项目内 `.tools/`，无需预装。

## 常用方式

直接双击：

```text
一键部署.cmd
```

只部署本机、不创建 GitHub 仓库：

```powershell
.\一键部署.cmd -LocalOnly
```

指定仓库、跳过首次业务运行：

```powershell
.\一键部署.cmd -Repository your-name/your-repo -Visibility private -SkipFirstRun
```

无人值守模式（需提前完成 `gh auth login` 或提供 `GH_TOKEN`，并通过当前进程环境变量提供 Secrets）：

```powershell
$env:GH_TOKEN = "你的 GitHub Token"
$env:DEEPSEEK_API_KEY = "你的 DeepSeek Key"
$env:SERVERCHAN_KEY = "你的 Server酱 SendKey"
powershell -ExecutionPolicy Bypass -File .\scripts\04_One-Click-Deploy.ps1 `
  -NonInteractive -Repository your-name/your-repo
```

不要把真实密钥写进命令脚本、Markdown、`.env` 或提交到 Git。
