# NEXT_PHASE_README

下一阶段才会生成正式给 Claude + DeepSeek V4 Flash 的 **Phase 0 指令**。

在此之前不要让 Claude 写业务代码。环境准备完成后，将以下信息回传给 ChatGPT：

- 项目路径；
- 环境检查脚本输出；
- `.venv` 是否成功；
- Claude Code 版本；
- DeepSeek API Key 是否已准备（仅“是/否”）。
